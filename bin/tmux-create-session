#!/bin/bash

function tmux-create-session() {
	# Check if tmux has a session
	tmux has-session 2> /dev/null
	if [ $? -eq 1 ]; then
		# Start a new session if tmux isn't running
		tmux new-session -d -c "$HOME" -n "system" -s "tmux" 2> /dev/null
	fi
}

tmux-create-session "$@"
