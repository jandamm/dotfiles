#!/usr/bin/env bash

if [ -n "$TMUX" ]; then
	exit
fi

function tmux-create-session() {
	# Check if tmux has a session
	tmux has-session 2> /dev/null
	if [ $? -eq 1 ]; then
		tmux
	else
		tmux attach
	fi
}

tmux-create-session "$@"
