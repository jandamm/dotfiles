#!/bin/bash

function activate-emacs() {
	if hash osascript 2>/dev/null
	then :
	else
		echo "No AppleScript available"
		exit 3
	fi

	osascript -e 'tell application "Emacs" to activate'
}

activate-emacs "$@"
