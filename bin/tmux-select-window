#!/bin/bash

# shellcheck source=../scripts/tmux_window_support.sh
source "$HOME/.scripts/tmux_window_support.sh"

function tmux-select-window() {
	local name
	name="$(tmux list-windows -F '#W' | grep "$1")"

	select_window "$name"
}

tmux-select-window "$@"
